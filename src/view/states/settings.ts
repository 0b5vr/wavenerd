import { atom } from 'recoil';

// == atoms ========================================================================================
export const settingsIsOpeningState = atom( {
  key: 'settingsIsOpeningState',
  default: false
} );
