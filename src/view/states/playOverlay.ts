import { atom } from 'recoil';

// == atoms ========================================================================================
export const playOverlayIsOpeningState = atom( {
  key: 'playOverlayIsOpeningState',
  default: true
} );
