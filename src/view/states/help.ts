import { atom } from 'recoil';

// == atoms ========================================================================================
export const helpIsOpeningState = atom( {
  key: 'helpIsOpeningState',
  default: false
} );
